/**
 * check the string is Empty or not
 * @param value string value
 * @returns Boolean true or flase
 */
const isEmptyString = (value: string): boolean => value.length === 0;

export default isEmptyString;
